// Styles
import styles from './Tabs.module.scss';

// Types
type TabsProps = {};

const Tabs = ({}: TabsProps) => {
  return (
    <div className={styles.tabs}>
      <p>Tabs</p>
    </div>
  );
};

export default Tabs;
