// Styles
import styles from './LabelValue.module.scss';

const LabelValue = () => {
  return (
    <div className={styles.labelValue}>
      <p>labelValue</p>
    </div>
  );
};

export default LabelValue;
